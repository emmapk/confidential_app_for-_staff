<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .profile-container {
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 8px;
            padding: 20px;
            width: 90%;
            max-width: 400px;
            text-align: center;
        }

        h1 {
            color: #333;
        }

        p {
            font-size: 16px;
            color: #555;
            margin: 10px 0;
        }

        .highlight {
            font-weight: bold;
            color: #007bff;
        }

        a {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        .profile-image {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <h1>Welcome, <span class="highlight"><%= user.fullname %></span>!</h1>

        <!-- Displaying User's Profile Image -->
        <% if (user.image) { %>
            <img src="/uploads/<%= user.image %>" alt="Profile Image" class="profile-image">
        <% } else { %>
            <img src="/uploads/default-avatar.png" alt="Default Avatar" class="profile-image">
        <% } %>

        <p>Address: <%= user.address %></p>
        <p>Next of Kin: <%= user.nextOfKin %></p>
        <p>Phone Number: <%= user.phoneNumber %></p>
        <p>Account Number: <%= user.accountNumber %></p>
        <p>Account Name: <%= user.accountName %></p>
        <p>Next of Kin Phone Number: <%= user.nextOfKinPhoneNumber %></p>
        
        <p><a href="/update-user/<%= user.email %>">Update User</a></p>
        <p><a href="/delete-user/<%= user.email %>">Delete User</a></p>
    </div>
</body>
</html>
